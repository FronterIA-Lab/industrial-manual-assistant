cd hybrid-rag-engine
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"

# 2. Copiar los archivos de código de arriba
# 3. Bajar PDFs de muestra (usar tus propios o descarga pública):
curl -L "https://some-public-legal-pdf.pdf" -o data/sample/doc1.pdf

# 4. Probar ingesta local:
cp .env.example .env  # agregar OPENAI_API_KEY
python -m src.ingestion.pipeline --docs ./data/sample

# 5. Probar query:
uvicorn src.api.main:app --reload &
curl -X POST http://localhost:8000/query \
  -H "Content-Type: application/json" \
  -d '{"question": "What are the contract termination conditions?"}'
